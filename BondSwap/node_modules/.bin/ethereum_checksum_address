#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../ethereum-checksum-address/bin/ethereum_checksum_address" "$@"
  ret=$?
else 
  node  "$basedir/../ethereum-checksum-address/bin/ethereum_checksum_address" "$@"
  ret=$?
fi
exit $ret
